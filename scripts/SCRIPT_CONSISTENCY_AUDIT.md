# Script Consistency Audit

Audit date: 2026-05-22  
Audit mode: static review only. No notebooks, manuscript files, supplementary source files, model training, result recomputation, or figure generation were run.

## Retained Public Files

The GitHub upload package now retains these public script files:

- `main_analysis_final.py`
- `figure_generation_cleaned.py`
- `build_supplementary_excel_A.py`
- `build_supplementary_excel_B.py`
- `build_supplementary_excel_C.py`
- `build_supplementary_material_docx.py`
- `README.md`
- `SCRIPT_CONSISTENCY_AUDIT.md`

The raw notebook-export transfer directory was removed from the upload package. The compact scaffold scripts were also removed from the upload package.

## Public Code Records

`main_analysis_final.py` is the manually organized final public analysis code exported from the finalized Jupyter workflow.

`figure_generation_cleaned.py` is the cleaned public code record exported from the figure-generation notebook.

Figure 1 was prepared separately as a schematic workflow figure and is not generated by Python. `figure_generation_cleaned.py` mainly preserves data-driven figure and panel generation code.

## Main Analysis Coverage

`main_analysis_final.py` retains the notebook-derived workflow for:

- data preprocessing and label construction;
- group-aware train/test splitting and `StratifiedGroupKFold`-based cross-validation;
- q0.75 primary high-delivery label construction;
- q0.70/q0.80 sensitivity definitions;
- model training and `Optuna` hyperparameter optimization;
- class-weight, none, and random-oversampling imbalance branches where present in the source workflow;
- LR, KNN, DT, SVM-RBF, LightGBM, XGBoost, `CatBoost`, DNN, Stacking, and Voting logic;
- CV metrics, OOF predictions, threshold selection, independent test evaluation, and primary-model selection;
- SHAP interpretation and one-hot feature aggregation;
- virtual candidate generation, deduplication, feasibility/OOD filtering, candidate scoring, top-candidate export, cancer-type-specific and condition-specific filtering, and local working-range estimation for Size, Zeta Potential, and Admin.

## Main-Script Keyword Review

Static keyword review confirmed that `main_analysis_final.py` retains or contains equivalent logic for:

- `StratifiedGroupKFold` and group-aware split;
- `Optuna`;
- `CatBoost`;
- OOF predictions and primary-model selection;
- `SHAP`;
- candidate generation;
- top-candidate export using related names such as `TOP_N`, `top_candidates`, `PAPER_TOPK_LIST`, `paper_candidate`, and `candidate_table`;
- cancer-type-specific or condition-specific filtering using related names such as `ENABLE_CT_SUBTABLES`, `CT_SUBTABLE_VALUES`, `RANGE_FILTERS`, and example cancer-type values such as `Breast`;
- local working-range estimation for `Size`, `Zeta Potential`, and `Admin`.

## Figure Generation Coverage

`figure_generation_cleaned.py` retains the final data-driven main-figure or corresponding panel-generation logic identified from the figure-generation notebook export.

Figure 1 was prepared separately as a schematic workflow figure and is not generated by Python. Broad exploratory EDA, old figure attempts, test figures, temporary plots, duplicate model-analysis cells, and duplicate virtual-screening cells were excluded from the retained public script.

## Supplementary Builders

The supplementary Excel and Word generation scripts are retained for supplementary-file organization:

- `build_supplementary_excel_A.py`
- `build_supplementary_excel_B.py`
- `build_supplementary_excel_C.py`
- `build_supplementary_material_docx.py`

Manual review remains recommended for workbook sheet contents, source-file availability, and formatting assumptions before regenerating supplementary files in a different environment.

## Static Checks Performed

- Confirmed the raw notebook-export transfer directory is not present.
- Confirmed the compact scaffold scripts are not present.
- Checked the full upload folder for Chinese characters.
- Checked the full upload folder for personal absolute paths and the legacy dated figure-output path.
- Checked for Chinese file names under the upload folder.
- Ran `python -m py_compile` on all retained Python scripts; no training, result computation, or figure generation was executed.

## Overall Judgment

**Ready after manual review.**

The repository now provides cleaned public code records exported from the main analysis and figure-generation notebooks, together with supplementary-file generation scripts. Manual review is still recommended before full re-execution in a different environment.

Remaining manual checks:

- verify final data-driven figure panel selection against the submitted or accepted figures;
- confirm raw-data access instructions and dependency versions;
- confirm supplementary workbook and Word-document builder inputs before regeneration;
- optionally wrap long-running top-level code in explicit `main()` or command-line guards for safer public use.
